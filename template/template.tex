\documentclass[12pt]{book}
\usepackage{xeCJK}
\usepackage{fontspec}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}  % Pandoc 可能生成 >{\raggedright\arraybackslash} 这种写法
\usepackage[strings]{underscore}  % 允许章节名里用下划线
% Pandoc 用到的 \tightlist 定义
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\geometry{a4paper, margin=2cm}

% 设置中文字体
\setCJKmainfont[Path=./chapters/fonts/]{NotoSansCJKsc-Regular}
\setCJKfamilyfont{hei}[Path=./chapters/fonts/]{NotoSansCJKsc-Bold}

% 使用 \textbf{} 时，Bold 字体会生效
\renewcommand{\bfdefault}{b}

\begin{document}

{% for part in parts %}
\part{ {{ part.title }} }
{{ part.readme_tex }}

{% for chapter in part.chapters_data %}
\chapter{ {{ chapter.title }} }
{{ chapter.tex }}
{% endfor %}

{% endfor %}

\end{document}
